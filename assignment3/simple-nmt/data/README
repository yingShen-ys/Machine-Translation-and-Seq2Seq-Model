/iwslt: original iwslt data with/without additional xnli data
Only used for pretraining. 

Running options:
--train ./data/iswlt/corpus.train \
--train ./data/iswlt/corpus_xnli.train \

--valid ./data/iswlt/corpus.valid \

/xnli_split: train/test split xnli data(0.9/0.1, not original validation/test split)
Running options:
--train ./data/nli_split/nli.train \
--valid ./data/nli_split/nli.valid \

/nli_iwslt: training data is all xlni data, validation data is iwslt validation
Running options:
--train ./data/nli_iswlt/nli.all \
--valid ./data/nli_iswlt/corpus.valid \

/xnli: raw xnli data
prepareData.py: python prepareData.py

examples usages at the end of the file:
files: xnli raw tsv files. Always ['xnli.dev.tsv', 'xnli.test.tsv'].
outputPrefix: output folder path.
splitRatio: train/test split. can be 1.0 if no split.
skipLabels: a set containing skipped labels.
prepareData(files, outputPrefix, splitRatio = 0.9, skipLabels = set())
